# 🍵 Фронтенд магазина чая

Современный React фронтенд для интернет-магазина чая.

## Технологии

- **React 18** + **TypeScript** - UI библиотека с типизацией
- **Vite** - быстрый сборщик
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **React Query** - управление серверным состоянием
- **Zustand** - управление клиентским состоянием
- **Axios** - HTTP клиент

## Структура проекта

```
frontend/
├── src/
│   ├── api/          # API клиенты (auth, goods, orders)
│   ├── components/   # Переиспользуемые компоненты
│   ├── pages/        # Страницы приложения
│   ├── store/        # Zustand stores (auth, cart)
│   ├── types/        # TypeScript типы
│   ├── App.tsx       # Главный компонент
│   └── main.tsx      # Точка входа
├── public/           # Статические файлы
└── package.json      # Зависимости
```

## Установка и запуск

### 1. Установите зависимости

```bash
cd frontend
npm install
```

### 2. Запустите dev сервер

```bash
npm run dev
```

Фронтенд будет доступен на `http://localhost:5173`

### 3. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## Переменные окружения

Создайте файл `.env` в корне `frontend/`:

```env
VITE_API_URL=http://localhost:8080/api/v1
```

## API Endpoints

Фронтенд использует следующие endpoints:

- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `GET /api/v1/users/me` - Информация о пользователе
- `GET /api/v1/goods` - Список товаров
- `GET /api/v1/goods/:id` - Детали товара
- `POST /api/v1/orders` - Создание заказа
- `GET /api/v1/orders/:id` - Детали заказа

## Функционал

- ✅ Каталог товаров с пагинацией
- ✅ Корзина покупок
- ✅ Регистрация и авторизация
- ✅ Создание заказов
- ✅ Просмотр деталей заказа
- ✅ Адаптивный дизайн

## Админ-панель

После авторизации доступна админ-панель по адресу `/admin` для управления товарами.

## Важно

Убедитесь, что API Gateway запущен на порту 8080 и CORS настроен правильно.

